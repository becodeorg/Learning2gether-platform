{% extends 'home.html.twig' %}

{% block title %}Learning2Gether{% endblock %}

{% block body %}
    <div class="sidebar">
        <img class="logo" src="{{ asset('assets/img/logo_transparent.png') }}" alt="logo">
        <div class="loginwrap">
            <a href="{{ path('app_login') }}"><button class="button_4">Login</button></a><br><br>
            <a href="{{ path('app_register') }}"><button class="button_4" >Register</button></a>
        </div>
    </div>
{% endblock %}
