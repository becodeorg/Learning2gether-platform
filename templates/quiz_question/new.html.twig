{% extends 'base.html.twig' %}

{% block title %}{{'New QuizQuestion' | trans }}{% endblock %}

{% block body %}
    <h1>{{'Create new QuizQuestion'  | trans }}</h1>

    {{ include('quiz_question/_form.html.twig') }}

    <a href="{{ path('quiz_show', {'id': quiz_question.quiz.id}) }}">{{ 'back to list' | trans }}</a>
{% endblock %}
