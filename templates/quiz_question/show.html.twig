{% extends 'base.html.twig' %}

{% block title %}{{ 'QuizQuestio' | trans }}n{% endblock %}

{% block body %}
    <h1>{{ 'QuizQuestion' | trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'Id' | trans }}</th>
                <td>{{ quiz_question.id }}</td>
            </tr>
            <tr>
                <th>{{ 'Question language code' | trans }}</th>
                <td>{{ app.user.language.code }}</td>
            </tr>
            <tr>
                <th>{{ 'Question' | trans }}</th>
                <td>{{ quiz_question.title(app.user.language) }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quiz_show', {'id': quiz_question.quiz.id}) }}">{{ 'back to list' | trans }}</a> |

    <a href="{{ path('quiz_question_edit', {'id': quiz_question.id}) }}">{{'edit' | trans }}</a>

    {{ include('quiz_question/_delete_form.html.twig') }}
{% endblock %}
